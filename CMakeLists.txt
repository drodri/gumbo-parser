
ADD_BII_TARGETS()
IF(WIN32)
	TARGET_INCLUDE_DIRECTORIES(${BII_BLOCK_TARGET} INTERFACE ${CMAKE_CURRENT_SOURCE_DIR}/visualc/include)
ELSE()
	TARGET_COMPILE_OPTIONS(${BII_BLOCK_TARGET} INTERFACE -std=c99)
ENDIF()